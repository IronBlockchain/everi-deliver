const camera = {
    init: 'CAMERA_init',
    move: 'CAMERA_move'
}

const user = {
    issue: 'USER_ISSUE',
}

const actionTypes = {
    camera,
    user
}

export default actionTypes;
export {camera, user}